services:
  - type: web
    name: phamdat-selfbot
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: DASHBOARD_PASSWORD
        sync: false
      - key: VOTE_SERVICE_URL
        sync: false
      - key: VOTE_SERVICE_SECRET
        sync: false
